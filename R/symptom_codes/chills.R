## Codes for chills


# Description:

desc <- "Chills"

tokens <- c('chills')

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("78064")))

icd10_codes <- as.character(children_safe(c("R6883")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
