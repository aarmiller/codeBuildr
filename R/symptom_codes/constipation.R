## Codes for constipation


# Description:

desc <- "Constipation"

tokens <- c('constipation')

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("5640")))

icd10_codes <- as.character(children_safe(c("K590")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
