## Codes for feeding_difficulties


# Description:

desc <- "Feeding difficulties and mismanagement"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("7833")))

icd10_codes <- as.character(children_safe(c("R633")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
