## Codes for fecal_impaction


# Description:

desc <- "Fecal impaction"

tokens <- c('fecal impaction')

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("56032")))

icd10_codes <- as.character(children_safe(c("K5641")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
