## Codes for pain_unspec


# Description:

desc <- "Pain Unspecified"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("33819","78096")))

icd10_codes <- as.character(children_safe(c("R52")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
