## Codes for jaundice


# Description:

desc <- "Jaundice (not of newborn)"

tokens <- c('jaundice (not of newborn)')

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("7824")))

icd10_codes <- as.character(children_safe(c("R17")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
