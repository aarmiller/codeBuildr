## Codes for paralysis


# Description:

desc <- "Paralysis"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("3449")))

icd10_codes <- as.character(children_safe(c("G83")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
