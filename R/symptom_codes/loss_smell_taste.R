## Codes for loss_smell_taste


# Description:

desc <- "Loss of Sense of Smell and Taste"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("7811")))

icd10_codes <- as.character(children_safe(c("R438")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
