## Codes for dehydration


# Description:

desc <- "Dehydration"

tokens <- c('dehydration')

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("27651")))

icd10_codes <- as.character(children_safe(c("E860")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
