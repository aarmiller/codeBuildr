## Codes for babesiosis


# Description:

desc <- "Babesiosis"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("08882")))

icd10_codes <- as.character(children_safe(c("B600")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
