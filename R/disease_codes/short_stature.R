## Codes for short_stature


# Description:

desc <- "Short stature"

tokens <- c('short stature')

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("78343")))

icd10_codes <- as.character(children_safe(c("R6252")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
