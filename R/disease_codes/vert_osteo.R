## Codes for vert_osteo


# Description:

desc <- "Vertebral Osteomyelitis"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("73028")))

icd10_codes <- as.character(children_safe(c("M4620")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
