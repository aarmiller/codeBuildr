## Codes for cryptococ


# Description:

desc <- "Cryptococcosis"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("1175", "3210")))

icd10_codes <- as.character(children_safe(c("B45")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
