## Codes for infertility_male


# Description:

desc <- "Male infertility"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("606")))

icd10_codes <- as.character(children_safe(c("N46")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
