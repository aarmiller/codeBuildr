## Codes for pancreatic_cysts


# Description:

desc <- "Cyst/pseudocyst of pancreas"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("5772")))

icd10_codes <- as.character(children_safe(c("K862","K863")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
