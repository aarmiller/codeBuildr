## Codes for scoliosis


# Description:

desc <- "Scoliosis"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("73730")))

icd10_codes <- as.character(children_safe(c("M4120")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
