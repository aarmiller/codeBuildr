## Codes for mycobact


# Description:

desc <- "Nontuberculous Mycobacterial Infection"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("031")))

icd10_codes <- as.character(children_safe(c("A31")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
