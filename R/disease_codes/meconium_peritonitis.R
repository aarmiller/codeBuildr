## Codes for meconium_peritonitis


# Description:

desc <- "Meconium peritonitis"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("7776")))

icd10_codes <- as.character(children_safe(c("P780")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
