## Codes for nephrolithiasis


# Description:

desc <- "Nephrolithiasis"

tokens <- c('nephrolithiasis')

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("5920")))

icd10_codes <- as.character(children_safe(c("N200")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
