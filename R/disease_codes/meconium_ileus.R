## Codes for meconium_ileus


# Description:

desc <- "Meconium obstruction in fetus or newborn"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("7771")))

icd10_codes <- as.character(children_safe(c("P760")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
