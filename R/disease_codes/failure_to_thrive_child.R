## Codes for failure_to_thrive_child


# Description:

desc <- "Failure to thrive (child)"

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("78341")))

icd10_codes <- as.character(children_safe(c("R6251")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
