## Codes for dengue


# Description:

desc <- "Dengue Fever"

# Diagnosis codes:

icd9_codes <- children_safe(c("061","0654"))

icd10_codes <- children_safe(c("A90","A91"))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
