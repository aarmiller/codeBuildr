## Codes for osteopenia


# Description:

desc <- "Osteopenia"

tokens <- c('osteopenia')

# Diagnosis codes:

icd9_codes <- as.character(children_safe(c("73390")))

icd10_codes <- as.character(children_safe(c("M8580")))

# Procedure codes:

pr_codes_icd9 <- c()

pr_codes_icd10 <- c()

pr_codes_cpt <- c()

# Medication codes

rx_codes <- c()
